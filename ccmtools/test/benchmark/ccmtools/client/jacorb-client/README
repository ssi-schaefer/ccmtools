Components.jar bauen
====================

$ cd Components
$ jidl -I. -d ./src CCM.idl
$ cd ..
$ mkdir build
$ javac -d build Components/src/Components/*.java


Benchmark Client Stubs bauen
============================
$ cd idl2
$ jidl -I/home/eteinik/install/ccmtools_install/include:. -d ./src *.idl
$ cd ..
$ mkdir build
$ javac -classpath ./build:$CLASSPATH -d build idl2/src/*.java



Benchmark Client kompilieren und starten
========================================
$ javac -classpath ./build:$CLASSPATH -d build src/*.java

$ jaco -classpath ./build:$CLASSPATH Client


Beachte in den IDL Files: 
 o) bei #include <...> muß ein Leerzeichen zwischen 'e <' sein
 o) mit #include <...> muß die -I Option verwendet werden
 o) Probleme wenn kein globaler Namespace verwendet wird 
    (import ClassName; führt zu einem Fehler)

   

