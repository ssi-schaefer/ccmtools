
# create local component
$ ccmtools-generate c++local -a -o xxx Hello.idl

# create mico stubs
$ ccmtools-generate idl2 -o xxx/CCM_Remote_Stubs Hello.idl

# create remote component
$ ccmtools-generate c++remote -o xxx Hello.idl

# create remote component test
$ ccmtools-generate c++remote-test -o xxx Hello.idl
$ cp 
$ cp 
$ confix.py --packageroot=`pwd`/xxx --bootstrap --configure --make --targets=check

# remove all generated code
$ rm -rf xxx ~/tmp/xxx