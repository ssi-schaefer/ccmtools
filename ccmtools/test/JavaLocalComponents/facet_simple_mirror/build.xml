<project name="JavaClientLibTest" default="compile">

  <property name="dest" location="xxx" />
  <property name="idl2" location="${dest}/idl2" />
  <property name="src-gen-local" location="${dest}/src-gen-local" />
  <property name="src-gen-remote" location="${dest}/src-gen-remote" />
  <property name="src-app" location="${dest}/src" />
  <property name="build" location="${dest}/build" />
  <property name="src" location="src" />

  <path id="compile.classpath">
      <pathelement path="${java.class.path}" />
  </path>


  <!-- Create the build directory-->
  <target name="init" description="" >
      <mkdir dir="${build}" />
  </target>
 

  <!-- Compile generated Java files-->
  <target name="compile" depends="init" description="" >
      <javac srcdir="${src-gen-local}:${src-gen-remote}:${src-app}:${src}" destdir="${build}" 
             debug="on" source="1.5" target="1.5">
          <classpath refid="compile.classpath" />  
      </javac>
  </target>


  <!-- Clean up -->
  <target name="clean" description="" >
      <delete dir="${build}" />
  </target>

</project>