#!/bin/sh

if [ -z "$CCMTOOLS_HOME" ]; then
echo "ERROR!"
echo "Environment variable CCMTOOLS_HOME not found!"
exit
fi

CLASSPATH=$CCMTOOLS_HOME/lib/ccm-runtime.jar:./xxx/build:$CLASSPATH

$JAVA_HOME/bin/java -enableassertions -cp $CLASSPATH DynamicClient
