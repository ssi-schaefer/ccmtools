#!/bin/sh

if [ -z "$JAVA_HOME" ]; then
echo "ERROR!"
echo "Environment variable JAVA_HOME not found!" 
exit
fi

if [ -z "$CCMTOOLS_HOME" ]; then
echo "ERROR!"
echo "Environment variable CCMTOOLS_HOME not found!" 
exit
fi

JAVA_LAUNCHER="$JAVA_HOME"/bin/java
if [ -x $JAVA_LAUNCHER ]; then 

CLASSPATH=$CCMTOOLS_HOME/lib/antlr.jar:$CCMTOOLS_HOME/lib/commons-cli-1.0.jar:$CCMTOOLS_HOME/lib/ccmtools.jar:CLASSPATH

$JAVA_HOME/bin/java -cp $CLASSPATH -Dccmtools.home=$CCMTOOLS_HOME -Djava.util.logging.config.file=$CCMTOOLS_HOME/etc/logging.properties ccmtools.parser.idl3.ui.Main $@  

else
echo "ERROR!"
echo "Java executable not found in" $JAVA_HOME/bin/java
fi