/*
 * This file was automatically generated by CCM Tools version 0.7.0
 * <http://ccmtools.sourceforge.net>
 * DO NOT EDIT!
 */
 
package application.ccm.remote;
                 
import java.util.logging.Logger;
import ccm.local.ServiceLocator;

import org.omg.CORBA.BAD_OPERATION;

import application.ccm.local.Group;
import application.ccm.local.InvalidPersonData;
import application.ccm.local.Login;
import application.ccm.local.PersonData;


/**
 * Interface adapter from CORBA to Java.
 */
public class LoginAdapterFromCorba
    extends application.LoginPOA
{
    private Logger logger = ServiceLocator.instance().getLogger();
    
    /** Java reference to the local interface */
    private Login localInterface;
	
    public LoginAdapterFromCorba(Login receptacle)
    {
        logger.fine("receptacle = " + receptacle + ")");
        this.localInterface = receptacle;
    }    	



    

    /** Business delegate method */
    public boolean isValidUser(application.PersonData person) 
        throws application.InvalidPersonData 
    {
        logger.fine("");
        
        try
        {
            // Convert CORBA parameters to local Java parameters 
            PersonData personLocal = application.ccm.remote.PersonDataCorbaConverter.convert(person);


            // Define local Java result type 
            boolean resultLocal;

            resultLocal = localInterface.isValidUser(personLocal); 
        
            // Convert local Java parameters to CORBA parameters
            
            

            // Convert local Java result to a CORBA result                 
            boolean result;
            result = (resultLocal);
            return result;

        }
	catch(InvalidPersonData eLocal)
	{
	    try
	    {
	        application.InvalidPersonData eRemote =
	             application.ccm.remote.InvalidPersonDataCorbaConverter.convert(eLocal);
	        throw eRemote;
	    }
	    catch(ccm.local.CorbaConverterException eConvert)
	    {
	        throw new BAD_OPERATION(eConvert.getMessage());
	    }
	}


        catch(java.lang.Exception e)
        {
    	    throw new BAD_OPERATION(e.getMessage());
        }
    }

}
