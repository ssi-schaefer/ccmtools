/***
 * CCM Tools Test Client 
 *
 * This file was automatically generated by %(CcmToolsVersion)s.
 *         <http://ccmtools.sourceforge.net/>
 *
 * This test client is part of the remote component test concept. 
 ***/

#ifdef HAVE_CONFIG_H
#  include <config.h>
#endif 

#ifdef HAVE_MICO 

#include <cstdlib> 
#include <iostream>
#include <string>

#include <ccmtools/remote/CCMContainer.h>

#include <CORBA.h>
#include <coss/CosNaming.h>

#include <%(RemoteIncludeNamespace)s%(HomeType)s_remote.h>
#include <%(StubsIncludeNamespace)s%(Identifier)s.h>

using namespace std;

//==============================================================================
// Implementation of remote client test
//==============================================================================

int main (int argc, char *argv[])
{
    cout << "Enter C++ remote test client" << endl;

    // Initialize ORB 
    int argc_ = 3;
    char* argv_[] = { "", "-ORBInitRef", "NameService=corbaloc:iiop:1.2@localhost:5050/NameService" }; 
    CORBA::ORB_var orb = CORBA::ORB_init(argc_, argv_);

    /**
     * Server-side code
     */ 

    // Register all value type factories with the ORB  
    ::ccmtools::remote::register_all_factories(orb);

    // Deploy local and remote component homes	
    int error = 0;
    error += deploy_%(AbsoluteLocalHomeName)s("%(HomeType)s");
    error += deploy_%(AbsoluteRemoteHomeName)s(orb, "%(HomeType)s");
    if(!error) 
    {
        cout << "%(HomeType)s server is running..." << endl;
    }
    else 
    {
        cerr << "ERROR: Can't deploy components!" << endl;
        return -1;
    }

    // For testing we use CORBA collocation	
    // orb->run();
	

    /**
     * Client-side code
     */
    CORBA::Object_var obj = orb->resolve_initial_references("NameService");
    CosNaming::NamingContextExt_var nc =
        CosNaming::NamingContextExt::_narrow(obj);

    // Deployment 

    // Find ComponentHomes in the Naming-Service
    obj = nc->resolve_str("%(HomeType)s");
    %(IdlHomeType)s_var my%(HomeType)s = 
        %(IdlHomeType)s::_narrow (obj);

    // Create component instances
    %(IdlIdentifier)s_var my%(Identifier)s = my%(HomeType)s->create();

    // Provide facets   
%(MProvidesDefFacetCreate)s
	
    my%(Identifier)s->configuration_complete();

    cout << "==== Begin Test Case ===================================" << endl;

    // TODO : IMPLEMENT ME HERE !       

    cout << "==== End Test Case =====================================" << endl; 

    // Destroy component instances
    my%(Identifier)s->remove();

    // Un-Deployment
    error  = undeploy_%(AbsoluteLocalHomeName)s("%(HomeType)s");
    error += undeploy_%(AbsoluteRemoteHomeName)s(orb, "%(HomeType)s");
    if(!error) 
    {
        cout << "%(HomeType)s server is running..." << endl;
    }
    else
    {
        cout << "Exit C++ remote test client" << endl; 	
    }
}

#endif // HAVE_MICO


