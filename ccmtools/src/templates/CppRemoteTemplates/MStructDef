// ===========================================================================
// struct adapter for %(Identifier)s (header file)
// ===========================================================================

#ifndef __STRUCT_ADAPTER_%(PreprocIdentifier)s__H__
#define __STRUCT_ADAPTER_%(PreprocIdentifier)s__H__

%(MFieldDefInclude)s

using namespace std;

%(OpenNamespace)s

/*
 * Convert a CORBA structure into a local structure.
 */

void CorbaConverter(const %(Identifier)s_ptr from, CCM_Local::%(Identifier)s& to);

/*
 * Convert a local structure into a CORBA structure.
 */

void CorbaConverter(const CCM_Local::%(Identifier)s& from, %(Identifier)s_ptr to);

%(CloseNamespace)s

#endif // __STRUCT_ADAPTER_%(PreprocIdentifier)s__H__



<<<<<<<SPLIT>>>>>>>
// ===========================================================================
// struct adapter for %(Identifier)s (implementation file)
// ===========================================================================

void
CorbaConverter(const %(Identifier)s_ptr from, CCM_Local::%(Identifier)s& to)
{
%(MFieldDef)s
}

void
CorbaConverter(const CCM_Local::%(Identifier)s& from, %(Identifier)s_ptr to)
{
%(MFieldDef)s	
}

