package ccm_runtime_remote
{
    depending on wx_utils;
    depending on mico;
    depending on ccm_runtime_local;

    installation Unix
    {
        // uses confix; creates 'configure', 'build' and 'install'
        standard;
    }
}
